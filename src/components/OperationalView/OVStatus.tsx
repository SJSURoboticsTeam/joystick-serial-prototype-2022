const OVStatus = ({ status }) => {
    return (
        <div id="ov-status">
            {/* <pre>{JSON.stringify(status, null, 2)}</pre> */}
            <p>Mode: <br/>
            Speed: <br/>
            Direction: </p> 
            {/* direction on compass */}
        </div>
    )
}

export default OVStatus;
